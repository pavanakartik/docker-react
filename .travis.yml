sudo: required

services:
    - docker
before_install:
    - docker build -t pavanakartik/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true pavanakartik/docker-react npm run test -- --coverage